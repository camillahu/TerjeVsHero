using ConsoleApp20;

Console.SetBufferSize(200, 60); // Ensure the buffer is at least as large as the window

Console.SetWindowSize(200, 60); // Set the window size (width, height)
Console.WriteLine(Console.WindowWidth);
Console.WriteLine(Console.WindowHeight);
App.Run();
